public class Addition implements MathOperation {
    @Override
    public double calculate(double left, double right) {
        // To perform an addition operation - change from hugobuta
        return left + right;
    }
}
